kind = "web"
previewPath = "/"
title = "Hill Furnishings Shipping API"
version = "1.0.0"
id = "artifacts/shipping-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19766

[services.development]
run = "pnpm --filter @workspace/shipping-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/shipping-app", "run", "build" ]
publicDir = "artifacts/shipping-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19766"
BASE_PATH = "/"
